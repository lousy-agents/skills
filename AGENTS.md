# Lousy Agents Skills - Codex and Copilot CLI Context

## Source of Truth
The shared contribution policy for this repository lives in `.github/copilot-instructions.md`.

Before contribution work, read `.github/copilot-instructions.md` and follow it as the canonical source for project philosophy, pull request requirements, validation gates, strict negative constraints, and before-commit workflow.

## Agentic Parity Mandate
When updating project conventions, pull request requirements, validation gates, or core mandates, make those changes in `.github/copilot-instructions.md`, not this file. This file should only contain Codex and Copilot CLI operational routing.

## Codex and Copilot CLI Workflow Instructions
- Use the available Agent Skills when their descriptions match the task.
- Use the configured MCP servers in `.mcp.json` when you need current documentation, lousy-agents analysis, or structured reasoning.
- Keep shared policy DRY: do not duplicate validation, PR, or commit rules here. Follow the "Before Commit" and "Mandatory" sections of `.github/copilot-instructions.md` for the canonical requirements.

## Commands
Use the commands documented in the canonical `.github/copilot-instructions.md` Commands section.

## Validation
Follow the canonical `.github/copilot-instructions.md` Validation section.

## Verification
Follow the canonical `.github/copilot-instructions.md` Verification section.

## Feedback Loop
Follow the canonical `.github/copilot-instructions.md` Feedback Loop section.

## Mandatory
Follow the canonical `.github/copilot-instructions.md` Mandatory section.

## Before Commit
Follow the canonical `.github/copilot-instructions.md` Before Commit section.

## Validation Suite
Follow the canonical `.github/copilot-instructions.md` Validation Suite section.
